<div class="hooyah-box col-10 offset-1">
    <div class="row">
        <div class="col-4">
            <%= image_tag 'user.png' %>
        </div>

        <div class="col-8">
            <h3>User: <%= @user.email %></h2>
            <h3>Member Since: <%= @user.created_at.strftime("%B %d, %Y") %></h3>
            <h4><%= @user.comments.count %> total comments </h4>
            <h4><%= @user.comments.where(rating: '🌟🌟🌟🌟🌟').count %> total 5 star reviews</h4>
        </div>
    </div>
    <div class="clear"></div>
</div>

<br /><br />
<h1 class="text-center">My Comments</h1>
<br /><br />
<div class="row">
    <% @user.comments.each do |comment| %>
        <div class="col-12 col-sm-6">
            <h2 class="text-center"><%= link_to comment.place.name, place_path(comment.place) %></h2>
            <blockquote>
                <% if comment.humanized_rating.present? %>
                <div class="clear">
                    <b> <%= comment.rating %> </b>
                </div>
                <div class="clear">
                    <b> <%= comment.message %> </b>
                </div>
                <div class="">
                    <em><%= comment.user.email %></em>
                </div>
                <% end %>
            </blockquote>
        </div>
    <% end %>
</div>
